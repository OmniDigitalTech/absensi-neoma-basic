version: '3.8'

services:
    # Other services (like Laravel, MySQL, etc.)

    mailhog:
        image: mailhog/mailhog
        container_name: mailhog
        ports:
            - "1025:1025" # SMTP
            - "8025:8025" # Web UI
